# Copy to /etc/trace.env, chmod 600, root-owned.
#
# This is the only place the key lives on the machine. It is not in the
# repository, not in config.toml, and not in any command line - systemd hands
# it to the process, so `ps` never shows it.
TRACE_X_BEARER=put-your-key-here
